export async function onRequest(context) {
  const { env } = context;
  const { DB } = env;
  const list = await DB.prepare(`SELECT * FROM events ORDER BY created_at DESC`).all();
  return Response.json(list.results || []);
}
